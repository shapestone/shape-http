name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          path: shape-http

      - name: Checkout shape-core
        uses: actions/checkout@v3
        with:
          repository: shapestone/shape-core
          path: shape-core

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          check-latest: true
          cache-dependency-path: shape-http/go.sum

      - name: Verify grammar files exist
        working-directory: shape-http
        run: |
          if [ ! -f docs/grammar/http.ebnf ]; then
            echo "Error: Grammar file missing at docs/grammar/http.ebnf"
            exit 1
          fi
          echo "✓ Grammar file exists (http.ebnf)"

      - name: Run tests
        working-directory: shape-http
        run: go test -v -race -coverprofile=coverage.out -coverpkg=./... ./internal/... ./pkg/...

      - name: Check coverage
        working-directory: shape-http
        run: |
          coverage=$(go tool cover -func=coverage.out | grep total | awk '{print substr($3, 1, length($3)-1)}')
          echo "Coverage: $coverage%"
          if (( $(echo "$coverage < 90.0" | bc -l) )); then
            echo "Warning: Coverage below 90% (got $coverage%)"
            echo "Guide recommendation: 95%+ for parser code"
          fi
          if (( $(echo "$coverage < 70.0" | bc -l) )); then
            echo "Error: Coverage critically low (minimum: 70%)"
            exit 1
          fi

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          files: ./shape-http/coverage.out
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
          verbose: true

      - name: Build
        working-directory: shape-http
        run: go build ./...

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          path: shape-http

      - name: Checkout shape-core
        uses: actions/checkout@v3
        with:
          repository: shapestone/shape-core
          path: shape-core

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          check-latest: true
          cache-dependency-path: shape-http/go.sum

      - name: Run linter
        uses: golangci/golangci-lint-action@v3
        with:
          version: latest
          working-directory: shape-http
          skip-pkg-cache: true
          skip-build-cache: true
